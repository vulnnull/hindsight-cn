# coding: utf-8

"""
    Hindsight HTTP API

    HTTP API for Hindsight

    The version of the OpenAPI document: 0.9.1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List
from typing import Optional, Set
from typing_extensions import Self

class TemporalWindow(BaseModel):
    """
    A caller-supplied window for recall's temporal arm.  Supplying this **replaces the date extraction** recall would otherwise run over the query text, so a caller that already knows the range it means does not have to phrase it in English and hope the parser agrees.  It is **not a filter**. The temporal arm is one of four retrieval arms: it surfaces memories whose own dates (``mentioned_at`` / ``occurred_start`` / ``occurred_end``) fall inside the window so fusion can rank them higher. The semantic, keyword and graph arms are unaffected, so results dated outside the window are still returned. Narrowing results to a date range is a different operation this does not provide.  Has no effect when the bank's ``enable_temporal_retrieval`` config is off: that flag gates the arm itself, and stays the single switch for it.
    """ # noqa: E501
    start: datetime = Field(description="Start of the window (inclusive).")
    end: datetime = Field(description="End of the window (inclusive).")
    __properties: ClassVar[List[str]] = ["start", "end"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TemporalWindow from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TemporalWindow from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "start": obj.get("start"),
            "end": obj.get("end")
        })
        return _obj


