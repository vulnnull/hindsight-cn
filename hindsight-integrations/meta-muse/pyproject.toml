[project]
name = "hindsight-meta-muse"
version = "0.1.0"
description = "Hindsight integration for Meta Muse — connect prompt, docs and an MCP endpoint preflight check"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.14.3",  # GHSA-cq5v-8q36-5273: OOB heap read in the C response parser
    "pydantic>=2.0",
]

[project.scripts]
hindsight-muse-preflight = "hindsight_meta_muse.preflight:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["hindsight_meta_muse"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.ruff]
line-length = 120
